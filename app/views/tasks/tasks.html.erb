<h1>My tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task_path(task.id) do %>
      <%= task.title %>
    <% end %>
  <%end%>
  </li>
</ul>
<%= link_to 'Create a new task' , new_path%>
